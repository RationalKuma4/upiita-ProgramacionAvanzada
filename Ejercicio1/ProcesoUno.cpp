#include <stdlib.h>
#include <unistd.h>
#include <sys\types.h>
#include <sys\wait.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
	if (argc != 2)
	{
		printf("Numero de parametros incorrectos. Uso %s [entero]\n", argv[0]);
		exit(EXIT_FAILURE);
		wait
	}


	return 0;
}